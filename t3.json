{
  "$schema": "https://t3.codes/schema/t3.json",
  "scripts": [
    {
      "name": "Setup Worktree",
      "command": "pnpm install && for file in .env .env.local; do if [ -f \"${T3CODE_PROJECT_ROOT:?}/$file\" ] && [ ! -e \"$file\" ] && [ ! -L \"$file\" ]; then ln -s \"$T3CODE_PROJECT_ROOT/$file\" \"$file\" || exit; fi; done",
      "icon": "configure",
      "runOnWorktreeCreate": true
    }
  ]
}
